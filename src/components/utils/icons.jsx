
import { CheckCircle, CheckCircle2, Check } from 'lucide-react';

// Fallback for CheckCircle icon across different lucide-react versions
export const CheckCircleIcon = CheckCircle || CheckCircle2 || Check;
