import { AlertCircle } from 'lucide-react';

export default function DatabaseErrorBanner({ message = null }) {
  // Base44 manages database connections automatically
  // This component is no longer needed but kept for backwards compatibility
  return null;

}